<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sundostudio.mapper.MemberMapper">
    
    <insert id="signUp">
        INSERT INTO member (memberName, memberId, memberPw, role)
        VALUES (#{memberName}, #{memberId}, #{memberPw}, #{role})
    </insert>

    <select id="findByMemberId" parameterType="string" resultType="com.sundostudio.dto.memberVO">
        SELECT*from tbl_member
        FROM member
        WHERE memberId = #{memberId}
    </select>

</mapper>